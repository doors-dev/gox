# v0.3.2

## Fixed

- Formatter: imports with a name — blank (`_ "embed"`), aliased (`f "fmt"`), and dot (`. "strings"`) — lost the space between name and path in `.gox` files.

## Docs

- Removed `goxx` references from README and llms.md.
